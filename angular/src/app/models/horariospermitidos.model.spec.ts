import { horariospermitidos } from './horariospermitidos.model';

describe('horariospermitidos', () => {
  it('should create an instance', () => {
    expect(new horariospermitidos()).toBeTruthy();
  });
});
